package com.xworkz.browser;
import com.xworkz.browser.*;

public class BrowserRunner {
	public static void main(String args[])
	{
		Browser browser=new MozillaBrowser();

browser.connect();
browser.developerTool();
browser.equals(browser);
browser.getClass();
browser.getCompany();
browser.getName();
browser.getVersion();
browser.getName();
browser.hashCode();
browser.notify();
browser.notifyAll();
browser.showTime();
browser.toString();
browser.cleanUp();
browser.Browser("google ","google 2.00","GOOGLE");
browser.wait(0, 0);
browser.wait(0);
browser.showTime();

}
}


